# Code Style Guide

- TypeScript strict mode enabled.
- Component names: PascalCase (e.g., `PageHeader.tsx`).
- Utility functions: camelCase with explicit return types.
- CSS: Tailwind CSS utility classes with `clsx` / `twMerge` via `cn()`. 
